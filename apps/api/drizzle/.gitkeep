# Drizzle migrations live here — generated by `bun run db:generate`.
# Keeps the folder present so the Docker build can COPY it even before any
# migration exists; the boot-time migrator treats an empty folder as a no-op.
